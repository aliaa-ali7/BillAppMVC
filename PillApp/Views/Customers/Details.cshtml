@model PillApp.Models.Customer

@{
    ViewData["Title"] = "Details";
}

<h1 class="btn btn-info text-white">Details</h1>

<div>
    <h4>Customer</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-success text-white">Edit</a> |
    <a asp-action="Index" class="btn btn-info text-white">Back to List</a>
</div>
